# 整合回 glitch-vn

這份寫到「yazelin 說做就能直接照著做」的程度。**現在不要做**，理由在第一節。

---

## 一、為什麼現在不做（2026-09-12 決定）

glitch-vn 在發佈流程的最後一哩：正篇剛發 release 4、市集簡介的天數今天才修好（線上仍是舊的
「十一天」，下次推才生效）、調查篇在收版控、`larch/stickers/` 還沒進版控。
這個時候把另一條線的 UI 改動推進去，任何狀況都會跟發佈攪在一起。

而且 `phone.html` 是遊戲裡的卡，換掉它等於動到玩家路徑，**改完要重跑一次完整實玩才能發，一趟一小時起跳**。

所以：畫面留在 glitch-live 繼續調，glitch-vn 一個字都不碰。等發佈那條線落地再照這份做。

---

## 二、動手之前（Larch 的三個坑，先做完再說）

1. **抓快照。** `PUT` 會清空專案，動手前先把整個專案 dump 下來
   （`larch/dump.py`，或 MCP 的 export）。
2. **關掉編輯器分頁。** Larch 的編輯器分頁開著會把版子打回舊版，推完必須對卡數，
   數字不對就是被打回去了，要重推。
3. **確認推的是哪一個專案。** 別推到 `reference_larch_sandbox_project` 那個沙盒。

---

## 二之二、誰動哪個檔（2026-09-12 對過）

`card.html` 是**整份覆蓋**，不是 patch，所以只要單邊寫就零衝突。分工：

| 誰 | 動什麼 |
|---|---|
| glitch-live 這邊 | `card.html` 整份。打 tag 給對方拉，**不要貼原始碼**——可稽核、可重拉、出事可比對 |
| glitch-vn 那邊 | `push.py` 三個新注入點、`card_test.mjs` 六條斷言、design 文件、變數表、實玩 |
| 兩邊 | 對方改完 `push.py`，回 glitch-live 再跑一次 `verify.mjs`（第一項會讀對方的 `push.py` 比對注入字串，十秒） |

反過來（對方改卡片、這邊審）會讓對方要重新推導卡片內部的契約——注入形狀、三個已讀變數、
`liveDays()` 讀 `phone_log`——那些脈絡在這邊。

### 要拉的那一版：`phone-v2`

```
https://raw.githubusercontent.com/yazelin/glitch-live/phone-v2/card.html
```

commit `c41116011a35ba5af5c2dd2faeda1d3e9b5d5676`（47,551 bytes）。
打 tag 時 `verify.mjs` 深淺各 23 項兩輪全過。
tag 上的說明寫了這一版是什麼、注入點怎麼動、讀寫哪些變數、`@` 那條契約。

線上那一份 2026-09-12 對過，跟這個 tag **逐 byte 相同**
（`node dev/check-live.mjs` 的 L1，sha `6ff82e27…`）。

**再要改就打 `phone-v3`，不要動已經發出去的 tag。**

> **【已推翻】** 這一節原本寫的是「要拉的那一版：`phone-v1`」，
> commit `b50f6b9`、22 項。**推翻它的是 w1D 讀補丁時提的問題**：
> `phone-v1` 的 `#reply` 送出鈕跟舊 `phone.html` 一樣是個空的 `<i>`，
> 而整合是整份覆蓋，拉 v1 會把 glitch-vn `c886435` 剛修好的送出鈕蓋回壞的。
> `phone-v2` 就是補那一處（移植 `c886435`），其餘完全相同。
> **舊網址那一行留在這裡是故意的**：它看起來完全正常，而且到今天都還取得到檔案，
> 所以只寫「改拉 v2」擋不住有人照舊連結去拉。

## 三、要換哪個檔

| 這裡 | 搬到那裡 | 怎麼搬 |
|---|---|---|
| `card.html` | `glitch-vn/larch/cards/phone.html` | **整份覆蓋。** 不要挑段落搬 |
| `index.html` | 不搬 | 預覽殼，扮演 Larch 當宿主用的 |
| `verify.mjs` | 不搬 | 留在 glitch-live |
| `assets/live-loop.mp4`、`assets/poster.webp` | 見第五節 | 還沒拍板 |

`dev/probe-sandbox-video.mjs` 是平台能力探測，不是卡片測試，所以另外放。

`ref/` 裡除了設計參考圖，還有一支沒有採用的循環影片（`ref/街景循環-未採用.mp4`）。
不採用的三個理由與「要用之前先處理什麼」寫在 `ref/README.md`，整合時不用管它。

`card.html` 是完整的卡片本體，不含手機殼、不含主控台。
Larch 裡卡片就是整個畫面，手機殼是預覽殼畫的。

---

## 四、注入點怎麼對應

`push.py` 用字串 `.replace()` 注入，**對不上不會報錯，只會整份原樣送上去**。
所以 `card.html` 裡每個 token 後面那段預設值，跟 `push.py` 的 `.replace()` 第一個參數一字不差。

`verify.mjs` 第一項就在驗這件事：它讀 `../glitch-vn/larch/inv/push.py`，
把 `phone_card()` 裡要換的字串逐一在 `card.html` 裡找。**改任何一邊之前先跑它。**

### 現有的五個，push.py 不用改

| token | card.html 裡長什麼樣 | push.py 現在換成什麼 |
|---|---|---|
| `MODE` | `/*@@MODE@@*/'full'` | `json.dumps(mode)` |
| `BANNER` | `/*@@BANNER@@*/null` | `json.dumps(banner)` |
| `POSTS` | `/*@@POSTS@@*/[]` | `json.dumps(posts)` |
| `OLD` | `/*@@OLD@@*/[]` | `json.dumps(old_thread)` |
| `AVATAR` | `/*@@AVATAR@@*/''` | `MAIN_ASSETS["avatar-glitch"]` |

沒注入的時候，`card.html` 用自己的 `*_DEFAULT` 跑起來，所以這個檔單獨開也是活的
（預覽殼就是這樣用）。注入之後那幾行 fallback 不會動到。

### 三個新的，push.py 要加

```python
.replace("/*@@VIDEO@@*/''",   json.dumps(MAIN_ASSETS.get("live-loop", "")))
.replace("/*@@COMPOSE@@*/true", json.dumps(COMPOSE_IN_CARD))   # 見第五節之二
.replace("/*@@NIGHTS@@*/null",  json.dumps(nights, ensure_ascii=False))  # 見下
```

### 貼文補到第十三天（2026-09-12 已進 glitch-vn）

`phone_feed()`（`push.py:754`）**直接解析 `design/調查篇-手機.md` 那張 markdown 表**，
所以補貼文只要改那張表，卡片與 `push.py` 都不用動。實跑過那支解析：
12 則全部解析出來、留言 ID 全都自帶 `@`、沒有吃到檔案裡別的表。

給之後還要補的人的三條限制：

1. **格式要一模一樣**：`| 天 | 貼文 | 指向 | 留言 |`，第一欄是裸數字。正規表示式是
   `^\| (\d+) \| (.+?) \| (.+?) \| (.+?) \|$`，**那個檔裡任何其他「四欄、第一欄是數字」
   的表都會被一起吃進去**。
2. **留言格式**：`@ID：文字`，多則用 `／` 隔開，時間戳寫 `@ID（02:40）：文字`。ID 要帶 `@`。
3. **不要寫第十四天的貼文。** 板只到第十三天，第十四天上午只有結局、不經過板，
   所以手機不會被打開，那一天的內容玩家看不到。

### 新增直播的晚上要做兩件事

卡片判「今晚有沒有開台」是讀 `phone_log` 裡那則「開始直播了」，不是照天數推。所以加一晚要：
`NIGHTS` 補那天的聊天室資料，**而且**建置層在那天晚上放橫幅卡。只做前者的話直播頁永遠是離線的。

`NIGHTS` 目前寫死在卡片裡，`/*@@NIGHTS@@*` `/` 那個注入點還沒用。哪天要加新的晚上，
建議一併改成注入（寫一支 `phone_nights()` 照 `phone_feed()` 的做法讀設計文件），
否則直播的台詞會同時存在兩個 repo——那是下面那個 bug 的近親。

### 一個踩過的坑：留言 ID 會變成 @@

`phone_feed()` 抓留言用 `(@\S+?)：`，**`@` 在捕獲組裡**，所以吐出來的 `comment.id` 自帶 `@`；
而「兩年前」那一串裡格莉奇自己那一行沒有 `@`。卡片如果自己再補一個，注入之後就會變成
`@@Bambi_Draft3`，格莉奇那行變成 `@格莉奇`。

**預設值跟注入值形狀一樣的話驗不出來**，所以 `verify.mjs` 有一項「注入形狀」，
直接用 `push.py` 會吐出來的形狀餵卡片。契約是：**POSTS／OLD 的 id 自帶 `@`，
聊天室 NIGHTS 的 id 不帶**（由 `post()` 自己補）。

### 一個拿掉的

舊卡片有 `/*@@LIVE_DAYS@@*` `/[2,5,8]`，`push.py` 其實沒有在注入它。新卡片把它拿掉了：
**哪幾晚開台改成讀 `phone_log` 裡「開始直播了」那幾則**，那是橫幅卡在一開板就寫進去的，
是遊戲真的寫下來的事。跟訊息同一個來源。

這一條有驗：`verify.mjs` 會把今晚那一則從 `phone_log` 抽掉，直播頁必須跟著變成離線、
「上次開台」退回前一場。照天數推的話這一項會過不了。

`NIGHTS`（三晚的標題、人數、聊天室）現在寫死在 `card.html` 裡。
**第一版可以不注入**，fallback 會用卡片裡那份，內容跟 `design/調查篇-直播.md` 一致。
等要把文字收回設計文件當單一事實來源，再寫一支 `phone_nights()`，
做法照現有的 `phone_feed()`。

### 讀寫變數

```python
"miniGameReadVars":  ["day", "slot", "phone_log",
                      "phone_day_seen", "phone_msg_seen", "phone_live_seen", "phone_theme"],
"miniGameWriteVars": ["phone_log", "open_phone",
                      "phone_day_seen", "phone_msg_seen", "phone_live_seen",
                      "live_comment", "phone_theme"],
```

跟現在比：

- `open_studio` 與 `met_櫃檯` 拿掉（訊息改讀 `phone_log`，不再自己推算）。
- `phone_msg_seen`（讀過幾則訊息）與 `phone_live_seen`（哪一天看過直播）是新的，**紅點靠它們**。
  舊卡片只有 `phone_day_seen`，而且 `#dot-msg` 那顆從來沒有被點亮過（`renderFull` 只設 `dot-feed`），
  等於死的 HTML。新卡片三顆都會亮，所以三個已讀狀態都必須存得住。
- `live_comment` 只有第五天的留言橋段會寫。
- `phone_theme`（`dark`／`light`）是手機上那顆深淺鈕。**那種 sandbox 裡 localStorage 存不了東西**
  （`dev/probe-sandbox-storage.mjs` 實測：localStorage／sessionStorage／indexedDB／cookie
  四個全丟 `SecurityError`），所以玩家的選擇只能走 Larch 變數。

**這三個變數 `design/調查篇-變數.md` 都還沒有。** 那張表目前 `phone_*` 只有 `phone_ringing` 一列
（而且它已經廢了），`phone_log`、`open_phone`、`phone_day_seen` 三個在用的都沒進表。
整合的時候一起補（第六節之十）。

---

## 五、三件的狀態（兩件已拍板，一件剩一個問題）

### 之一、影片放哪（部分已驗，剩一件要拍板）

`assets/live-loop.mp4` 是 720×1280、10 秒、**3.08 MB**，首尾同幀可以無縫循環。

#### 已經驗掉的：卡片載得動外部影片

Larch 的 miniGame 與插件卡跑在 `srcdoc` + `sandbox="allow-scripts"` 的 iframe 裡，
**origin 是 `null`**（opaque）。那種環境有一串東西一碰就丟 `SecurityError`
（`localStorage`、IndexedDB、Service Worker），所以「影片載不載得動」不能用常識推。

`dev/probe-sandbox-video.mjs` 在本機把同樣的條件重現了一次：同一支 mp4 放在另一個 port
（對 iframe 而言就是跨網域，跟 R2 一樣），塞進 `sandbox="allow-scripts"` 的 srcdoc iframe。

| 量到什麼 | 結果 |
|---|---|
| iframe 的 origin | `null`（確認條件對了） |
| `muted` 自動播放 | 過（Larch 裡沒有使用者手勢，所以這一項是必要條件） |
| `readyState` / 播到第幾秒 | 4 / 3.23 |
| 解析度 | 720×1280 |
| 錯誤 | 無 |

**所以「卡片裡播影片」這件事本身沒有問題。** 剩下的只是檔案放哪。

#### 兩條路，差在哪

| | Larch 素材庫 | 外部網址（glitch-live 的 GitHub Pages） |
|---|---|---|
| 要不要上傳 | 要 | **不用，現在就有穩定網址** |
| 網址會不會變 | R2 固定網址，跟立繪同一個地方 | 跟著 repo，改名就會斷 |
| 資產集中 | 跟作品在一起 | 散在兩個地方 |
| 還沒驗的 | `POST /media` 吃不吃 `video/mp4` | 無 |

#### 上傳那條路還沒驗的兩件（要先問過才動）

1. **`POST /media` 吃不吃 `video/mp4`。** 那支的 `category` 只認 `scene`／`character`／`prop`，
   素材包那邊也只有 `music`／`sfx`／`ui`／`emoji`／`general`——**清單裡沒有影片這一類**。
   上限是 50 MB，3.08 MB 過得去；卡住的會是型別。
2. **這支是整條線上最慢的端點。** 它把 base64 穿過 API 伺服器，而且每傳一張就改寫一次
   整份專案文件；27 張 80 KB 的 WebP 序列跑推估 22 分鐘。3.08 MB 的 mp4（base64 約 4.1 MB）
   會更糟，很可能吃到 502。**而且 502 不等於沒寫入**——`POST /media` 收到 502 一定要
   `GET` 回來查，直接重試會產生重複素材。

**這兩件要實際傳一次才知道，而那是對 Larch 的寫入動作。** 要驗的話應該在沙盒專案
（`project-5aae449c…`）傳，不要拿 glitch-vn 那個專案試。**動手前要先問過。**

#### 已拍板：走外部網址（2026-09-12）

卡片的 `VIDEO` 注入成這個絕對網址：

```
https://yazelin.github.io/glitch-live/assets/live-loop.mp4
```

上傳素材庫那條留到整合那天再驗——**現在傳沙盒等於提前做整合日才要做的事，而整合被押後
（等 glitch-vn 的發佈線落地），方案還可能變，提前驗可能白做。**

#### 這條決定帶來一個跨 repo 依賴，要有人擋

glitch-vn 的正式專案從此指著 glitch-live 的一個檔案。**那個 repo 改名、轉私有、刪掉，
或是那個檔案改名搬走，遊戲裡的直播畫面就會壞掉，而且不會報錯——玩家看到的是一片黑。**

寫在文件裡不夠：三個月後不會有人讀文件，會碰到它的是那天想整理 repo 的人。
所以做了三層，一層比一層被動：

| 層 | 做了沒 | 誰會看到 | 擋得住什麼 |
|---|---|---|---|
| glitch-live 的 `README.md` 最上面一段警告 | **做了** | 打開 repo 首頁的人 | 要整理 repo 的人第一眼 |
| `assets/README.md` | **做了** | 在 GitHub 上點開 `assets/` 資料夾的人（會直接渲染在檔案清單底下） | 要動那個檔案的人 |
| glitch-live 的 `verify.mjs` 加一項：線上那個網址回不回 200 | **做了** | 跑驗收的人 | 檔案被改名或搬走 |

**三層都擋不到「整個 repo 被刪掉」**，因為那時候上面三樣一起沒了。
那一種只有消費端救得到，所以下面這一條是**整合日的必做步驟，不是選配**。

#### 整合日必做：在 glitch-vn 那邊加一條消費端檢查

加進 `glitch-vn/tools/card_test.mjs`（或任何一支每次都會跑的驗收腳本）：

```js
// 手機直播頁的影片在別的 repo 上（glitch-live）。那邊被刪掉或改名，
// 這裡的遊戲就是一片黑而且不報錯，所以在這裡叫。
{
  const url = 'https://yazelin.github.io/glitch-live/assets/live-loop.mp4';
  try {
    const ac = new AbortController();
    const t = setTimeout(() => ac.abort(), 8000);
    const r = await fetch(url, { method: 'HEAD', signal: ac.signal });
    clearTimeout(t);
    ok(`手機直播的影片還在（${url}）`, r.status === 200, `回 ${r.status}`);
  } catch (e) {
    console.log(`  （連不出去，跳過影片檢查：${e.name}）`);
  }
}
```

**這一條寫在這裡是有原因的。** 今天看到過同一類的東西：`glitch-vn/larch/stickers/`
是已經上線的素材，卻只存在硬碟上，沒有任何機制會提醒。**靠文件記住的東西，時間久了都會失效。**

順帶一提為什麼不用排程的 GitHub Actions 去 ping：**排程 workflow 在 repo 六十天沒有提交之後
會被 GitHub 自動停用**，那正好是同一個毛病再上一層——一個會自己安靜死掉的守衛，
比沒有守衛更糟，因為你以為有人在看。

#### 哪天改走 Larch 素材庫

這條依賴就沒了。要拿掉的東西：glitch-live 的 README 警告、`assets/README.md` 那一段、
兩邊 verify 的網址檢查。

---

### 之二、第五天的留言橋段（已拍板：留在卡片裡）

**2026-09-12 拍板：留在卡片裡。** `COMPOSE` 注入成 `true`（也是卡片的預設值），不用改。

做法：輸入框亮起來 → 玩家點下去 → 看她打「你記得昨天講到哪嗎」、停一下、把後半句刪掉
改成「開場講了什麼嗎」→ 兩個按鈕「送出去」與「不打」。

- 按「送出去」：卡片 `setVar('live_comment', true)`，那一行進聊天室，四秒之後滾掉。
- 按「不打」：卡片 `setVar('live_comment', false)`。

板子照 `live_comment` 分到五之一或五之二。`design/調查篇-直播.md` 第五天那個 choice
的**文字不再放在板上**，改由卡片演——這一點要在那份設計文件裡註明，
不然以後有人照舊文件去板上找那個 choice 會找不到。

`live_comment` 要進 `miniGameWriteVars`，也要補進 `design/調查篇-變數.md` 的變數表。

---

### 之三、電話那一格（已拍板：拿掉）

**2026-09-12 yazelin 拍板：拿掉那一格，直播加紅點。** glitch-live 已經照做，以下是依據與連帶改動。

#### 為什麼拿掉

那一格在整個調查篇裡沒有任何資料來源：`drawCall` 是寫死的兩行字，`push.py` 的 `phone_card()`
沒有注入電話內容的地方，`design/調查篇-通關路線.txt` 裡「沒有人會打來」出現 0 次
（自動玩家十三天一次都沒翻到）。

而且它是**遺留物不是設計**，證據在 `design/調查篇-變數.md:192`：

> `phone_ringing`｜布林｜手機的使用條件｜沒有人寫。永遠 false，所以手機永遠用不了，訊息「沒有人會打來。」

那句話原本的工作是**解釋這個道具為什麼按不下去**。`調查篇-手機.md`（09-08）把手機改成真道具、
拿掉 `phone_ringing` 之後，那個工作就沒有了，可是那句話被留下來還分到一整格。

另外它跟 `調查篇.md` 零的判準對不上。零那張表的「寫錯的」欄寫著「任何在寫他寂寞、他孤單、
城市很冷的句子」，舉的例子是「有人來接她走，沒有人來接他。」——同一個句型，
而且它沒有在講重複或者被記住。

一句話：**有意義的空只需要被看到一次。** 那句話回 `調查篇-背包與謎題.md` 二之一那張表的備註欄
（本來就還寫著它），玩家點道具、看到一次、懂了。放在分頁裡是每天被點一次、每天都一樣。

#### 拿掉牽動哪些地方（不只是刪一個 drawCall）

glitch-live 這邊已經做完：

| 動了什麼 | 檔 |
|---|---|
| 分頁按鈕、`drawCall`、`PAGES` 對應表 | `card.html` |
| 開頭註解「四頁」改「三頁」、讀寫變數清單 | `card.html` |
| 頁面描述「四頁」改「三頁」 | `index.html` |
| `page-call` 那一項拿掉，改成驗「分頁剩三格且找不到那兩行字」 | `verify.mjs` |
| 四頁的敘述 | `README.md`、`NEXT.md`、這一份 |

**`push.py` 不用改**：整份 grep 過，跟分頁數有關的東西一個都沒有
（唯一命中的 `bg-curtain-call` 是謝幕背景，無關）。

**`tools/card_test.mjs` 要改，而且不只電話那一條。** 那支測試對舊卡片的 DOM 寫死了選擇器，
新卡片有六條會壞：

| 行 | 現在寫的 | 為什麼壞 | 改成 |
|---|---|---|---|
| 386 | `nav button` hasText `電話` | 分頁沒了 | 整條刪掉 |
| 387 | `.call` 含「沒有人會打來」 | 那一頁沒了 | 整條刪掉，改驗「找不到這句」 |
| 384 | `.live p` | 離線直播頁的 class 改成 `.offair p` | `.offair p` |
| 384 | 含「第 2 天」 | 改用國字了 | 「第二天」 |
| 394 | 含「今晚開台」 | 開台時直接進直播介面，沒有那句文字 | 改驗 `#screen` 沒有 `page-on` |
| 388 | `#close` | 收起來搬進分頁列第五格 | `#t-close` |

順帶：`nav button.on`、`.post`、`.old .id.del`、`.cm .c`、`#reply span`、`#dot-feed`、
`#s-who`、`#s-txt` 這幾個選擇器新卡片都還在，不用動。

#### 連帶：直播那一格加紅點（一起做完了）

分頁列唯一的新鮮度訊號是紅點，而它本來只有訊息與格莉奇有。直播跟電話都沒有。
所以電話真正在教的是「沒有紅點的分頁不用看」，而直播沒有紅點、卻在三個晚上真的有東西——
**實際損失是玩家被訓練到錯過直播。**

新卡片三顆紅點：

| 分頁 | 什麼時候亮 | 看過之後寫什麼 |
|---|---|---|
| 訊息 | `phone_log` 的則數 > `phone_msg_seen` | `phone_msg_seen` ← 目前則數 |
| 格莉奇 | 有 `day <= 今天` 而且 `day > phone_day_seen` 的貼文 | `phone_day_seen` ← 今天 |
| 直播 | 今晚在開台，而且 `phone_live_seen !== 今天` | `phone_live_seen` ← 今天 |

兩條規矩：

1. **都掛變數不掛天數。** 直播那顆是「今晚在不在開台」，而「今晚開不開台」讀的是 `phone_log`
   裡今晚那一則「開始直播了」，不是卡片自己照 `[2,5,8]` 推。跟訊息同一個來源。
2. **已讀一定要寫回 Larch。** 不寫回去的話卡片一重新掛載紅點就又亮了，
   玩家會學到紅點沒有意義，那比沒有紅點更糟。舊卡片的 `#dot-msg` 就是這個毛病
   （它其實從來沒被點亮過，所以沒人發現）。`verify.mjs` 有一項專門驗這件事：
   清掉已讀 → 紅點回來 → 逐頁看過 → 都滅 → 重新掛載 → 不該再亮。

訊息那一顆有個例外：手機一打開就落在訊息頁（有訊息的話），所以它是當場被讀掉的，
開場就不會亮。要驗的是它有沒有把 `phone_msg_seen` 寫回去。

#### 備案：那通始終沒有打來的電話（新正典，要 yazelin 定）

今天選的是拿掉，但這個點子比空殼好，記在這裡不要弄丟：

公關窗口回過「您的來信已收到，我們將於三至五個工作天內回覆。」——**那通電話始終沒有打來。**
一通未接來電，或者一次都沒有，是在講「一直等」，過得了 `調查篇.md` 零的判準，
也接得上既有的線（`met_櫃檯` 已經有了）。

要做的話它不該是一個常駐分頁，而是訊息頁底下一則「未接來電（0 通）」之類的東西，
或者第 N 天之後才出現的一則。**那是新正典，要 yazelin 決定要不要寫、寫成什麼。**

---

## 五之三之二、盲測那份數據怎麼讀（方法論限制，不是對稿子的評價）

第 9–13 天的貼文補完之後做過一次盲測：把五則原作跟五則新稿打散，丟給三個不同家族的模型
（glm-5.3、qwen3.5:397b、mistral-large-3:675b），問哪幾則是模仿的。
十次指認只有兩次指到新的，亂猜的期望值是五次。

**那是指認失敗，不是「新稿寫得好」的直接證據。** 兩者相關但不等價：
一組全都平庸到無從分辨，也會得到同樣的數字。低於亂猜只能說明「模型分不出來」。

真正有訊息量的是第二件：**三家判「假」的理由都是同一類——太溫暖、太完整——
而它們指的偏偏是她真的寫的那幾則**（三家一致把板上原本就有的
「今晚也會開台。我會記得你們的。」判成模仿）。那說明模型對這個角色的先驗就是**乾而窄**，
而新稿正好落在那個先驗裡，所以分不出來。作者照這個把第 11 與第 13 則改得更具體、更像事件。

**人讀起來像不像，只有 yazelin 判得了。** 這一節記的是量測方法的限制，
不要拿它當「稿子沒問題」的證據。

## 五之四、一個設計觀察（記著，現在不做）

**這支手機目前對玩家是完全唯讀的。** 除了切分頁與那顆深淺鈕，玩家從頭到尾只能看：
貼文點不開、聊天室滑不動、訊息回不了（那一條是設計，`調查篇-背包與謎題.md` 五）。

深淺鈕就是為了這件事做的——yazelin 2026-09-12：「玩家切深淺其實就是一個小按鈕，
讓玩家能夠和這個手機有互動效果而已。因為目前為止玩家都沒和這支手機有任何互動效果。」

之後如果要加別的互動（點貼文看大圖、往下滑載入更多、對某則按讚），從這句話開始。
**現在不做，這是觀察不是待辦。**

## 六、整合之後的驗收

**這份要整份跑完才算完成，不是挑幾項。**

### 機器驗

1. `cd glitch-live && node verify.mjs` —— 十五項全過（注入點、三晚的浮水印、留言橋段、
   三頁的字、紅點與已讀、開台讀 phone_log、訊息則數、貼文天數、收起來的變數、橫幅模式）。
   注入點那一項會去讀 glitch-vn 的 `push.py`，所以改完 push.py 要回來再跑一次。
2. `python3 larch/inv/push.py`（或 `build_all.py`）跑得過，且**推完對卡數**——
   編輯器分頁會把版子打回舊版，卡數不對就是被打回去了。
3. `node tools/card_test.mjs` —— **先照五之三那張表改六條斷言**，再跑：三頁切換、貼文照 `day` 出現、
   留言的 ID 與口氣、兩年前那一串在最底下、已刪除的標記、
   橫幅模式兩秒自己走、從背包打開與關掉時 `open_phone` 的寫回。
4. `python3 tools/pathlint.py` —— 板子沒斷軌。
5. **重跑一次完整實玩**：`node tools/autoplay.mjs` 走完十三天，
   **而且 `autoplay.mjs` 本身要先改**——它的手機快照是真的去驅動卡片 DOM，
   用的是舊卡片的選擇器，不改的話逐字稿每天都會變成「[手機] 打不開」而且腳本 exit 0。
   逐條在 [`交辦-重產通關路線基準.md`](交辦-重產通關路線基準.md) 第四節。
   基準本身要在整合**之前**重產，規格在同一份文件。
   （原本那句：）
   transcript 裡每天的 `[手機]` 快照要跟 `design/調查篇-通關路線.txt` 現在那份對得起來
   （則數、貼文數）。對不上就是手機的讀取邏輯換掉之後行為變了。
   **這一趟一小時起跳，不能跳過**——`phone.html` 是玩家路徑上的卡。

### 人驗

6. 真的用滑鼠把三頁點一遍，三晚各點一次，第五天的輸入框按一次，
   而且**跨兩次開關手機看紅點**：第一次看過之後關掉，第二次打開不該再亮。
7. 橫幅模式的透明：`調查篇-手機.md` 六說有的裝置畫不出透明（片尾字卷在他的機器上就白了）。
   **要在他自己的機器上看**：透得出來就留，白了就把 `card.html` 的
   `body.banner{background:transparent}` 換成 `rgba(6,8,12,.35)`。
   預覽殼的「收到訊息的橫幅」按鈕會在 iframe 後面放一張圖，透得出來就看得到她。
8. 影片在真機上會不會自動播（`muted` 才播得動，所以預設靜音）。

### 回頭補的

9. `glitch-vn/design/調查篇-手機.md` 要更新：二之四那一節（電話）整節刪掉、
   四頁改三頁、紅點那三條規矩、`live_comment` 這個新變數。
10. `glitch-vn/design/調查篇-背包與謎題.md` 二之一那張表的手機備註欄確認還是
    「沒有人會打來。」——那句話回到那裡就是它原本的位置，不用改，只要確認沒被刪掉。
11. `glitch-vn/design/調查篇-變數.md` 的變數表補齊：現在在用卻沒進表的
    `phone_log`、`open_phone`、`phone_day_seen`，加上新的 `phone_msg_seen`、
    `phone_live_seen`、`live_comment`；`phone_ringing` 那一列標成已廢。
12. glitch-live 的 `NEXT.md` 把做完的劃掉。
