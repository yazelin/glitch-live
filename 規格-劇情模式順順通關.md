# 規格：劇情模式要能順順通關

本人的原話是「至少也要劇情模式能順順通關」。這一份把那句話變成可執行的判準。

**先講結論**：分三層，三層要分開跑、分開紅。前兩層現有工具大致夠用，**第三層現在驗不到**，
要新做一支。三層各自的負控制都不能省，而且**負控制要跑在真資料上**——
2026-09-12 已經栽過一次：合成 fixture 不同形，負控制在上面全綠，工具本身卻是假綠的。

---

## 零、為什麼要分三層

「順順通關」不是二元狀態。它至少有三層，而且**前兩層過了看起來就像通關了**，
所以第三層最容易被漏掉：

| | 這一層在問什麼 | 壞掉的話玩家會怎樣 |
|---|---|---|
| 一、走得完 | 會不會卡死、會不會進到沒有出口的狀態、走不走得到謝幕 | 玩不下去 |
| 二、走得對 | 被帶著走的那條路跟軌道一不一致 | 走得完，但走的是另一條路，該觸發的沒觸發 |
| 三、走起來不斷 | 該有的東西在該出現的時候有沒有出現 | 走得完也走得對，但一路上有洞：沒有背景、沒有配樂、沒有聲音、手機是凍的 |

---

## 一、走得完

### 判準

| # | 判準 | 資料從哪來 |
|---|---|---|
| 1-1 | `pathlint.py` 零條 | `larch/inv/out/payload.json`（板子的靜態結構） |
| 1-2 | 逐字稿最後有 `=== 走到謝幕（遊戲結束）` | 實跑的逐字稿 |
| 1-3 | 逐字稿裡 `[卡住]` 出現 0 次 | 同上 |
| 1-4 | 最後一個板標頭是 `=== 板 第 13 天`（第十四天上午只有結局，不經過板） | 同上 |

### 怎麼跑

```bash
cd ~/glitch-vn
python3 tools/pathlint.py                      # 1-1，靜態，秒級
MODE=story OUT=/tmp/story-run node tools/autoplay.mjs   # 其餘三項，一趟四十分鐘起跳
```

其餘環境變數照 `design/調查篇-通關路線.txt` 檔頭那份清單（十個，沒設的也列出來），
**只有 `MODE` 換成 `story`**。

### 一個要先修的不一致

`tools/playrate.sh` 第 10 行把「好結局」定義成逐字稿裡有 `選「把筆記寫完」`。
**可是現有的完美通關基準裡那句出現 0 次**，它走的是 `選「翻到倒數第三頁」`，
而且最後確實走到了 `=== 走到謝幕（遊戲結束）`。

所以那兩個判準現在互相矛盾：照 `playrate.sh` 的分類，這一輪會被判成「短結局」。
**這道關不要用 `playrate.sh` 的分類當判準**，用 `=== 走到謝幕（遊戲結束）`。
`playrate.sh` 那條要嘛修、要嘛註明它量的是別的東西——但那是另一件事，不要夾在這道關裡。

### 負控制

在 `payload.json` 的**副本**上動手，不要推上去：

1. 挑軌道上任何一天的出邊，把它的 `condition` 改成永不成立（例如 `day eq 999`）。
2. `python3 tools/pathlint.py <改過的副本>` 應該報「軌道斷了　第 N 天 時段 M」。
3. 改回來。

另一半（動態那三項）用改逐字稿驗：把 `=== 走到謝幕（遊戲結束）` 那一行刪掉，
或塞一行 `  [卡住] …` 進去，閘要紅。

---

## 二、走得對

### 判準

`tools/railcheck.py` 拿實跑逐字稿**逐步**比軌道：第 N 天第幾個時段去了哪裡、
在選單上選了哪一格，一步一行。

```bash
python3 tools/railcheck.py /tmp/story-run/transcript.txt
```

現況是 **47/48**，差的那一步是擲骰不是 bug。

### 這個數字要處理，不然它會變成盲點

「47/48」現在是口耳相傳的常態。下一次變成 46/48 的時候，沒有人分得出來
「又是擲骰」還是「真的偏了一步」。用「≥47」當門檻會把新的偏差吃掉。**判準要寫成：**

> 允許的不一致要**逐條列出來**，每一條註明原因（哪一天哪個時段、為什麼是擲骰造成的）。
> 跑完之後：**清單裡沒有的不一致一律當紅**，清單裡的條數**不得增加**。

那份清單要進版控（建議放 `design/調查篇-難度.md` 或 `railcheck.py` 檔頭），
不要留在對話或記憶裡。

### 名單 6/6 是另一件事，而且工具現在在 /tmp

結局名單幾分之六是 `score.py` 在算，而它住在一個 **session 綁定的 scratchpad**
（`/tmp/claude-1000/-home-ct/7955990f-…/scratchpad/score.py`，2026-09-12 查還在）。
`/tmp` 會被清、session 會結束。

**這道關要用到它，就要先把它搬進 repo**（`tools/`），不然這一項的判準寫了也跑不了。
搬之前先確認它現在還跑得動。

### 負控制

拿真的逐字稿改：把某一步的 `→ 選「…」` 換成別的選項名，`railcheck.py` 必須**指名那一步**。
只報「對上 46/48」不算過——要能指出是哪一步，否則下次真的偏了也只會看到一個數字變小。

---

## 三、走起來不斷

### 先講一件事實：現在驗不到

我查過 `tools/autoplay.mjs` 印出來的標記只有六種：

```
[卡住] [手機] [第一頁] [背包] [選項] [錄音帶]
```

**背景、BGM、語音一個都沒有記。** 所以這一層**不可能從現有逐字稿驗**——
那份資料裡根本沒有那些欄位，補再多判準也讀不到。

有兩條路，建議走第二條：

| | 做法 | 代價 |
|---|---|---|
| 甲 | 在 `autoplay.mjs` 裡加記錄（每張卡印背景／BGM／有沒有 voiceUrl） | 要動那支腳本，而且每驗一次都要跑四十分鐘 |
| 乙 | **靜態走軌道**：從 `payload.json` 把劇情模式那條路走一遍，逐張卡檢查欄位 | 秒級、可重複、不用跑遊戲 |

乙的前提是「軌道是靜態可解的」，而那已經成立——`larch/inv/build.py` 第 1071 行就是
從 `design/調查篇-通關路線.txt` 解出軌道來組劇情模式的。**同一份解析可以重用。**

### 判準（四項，全部靜態）

| # | 判準 | 資料從哪來 |
|---|---|---|
| 3-1 | **背景**：軌道上每一張會換場的卡，`background` 是解析得到的網址（非空、非 `none`、非本機路徑） | `payload.json` |
| 3-2 | **BGM**：每一個該換曲的點都有 `bgm`；要靜音的地方掛的是**真的無聲音軌**不是空字串（空字串會被當成沒設，音樂會繼續播） | `payload.json` |
| 3-3 | **語音**：軌道上每一句該有聲的都有 `voiceUrl`；那張卡的 `voiceMode` 不是 `off`；**而且 `project.languages[].voiceMode` 有設**（線上與匯出版是兩道閘，各要各的） | `payload.json` ＋ `art/voice/spoken.json` |
| 3-4 | **手機**：`~/glitch-live/dev/check-transcript.mjs`（已經有了，四點加負控制） | 實跑的逐字稿 |

3-4 是既有的，可以直接當這一層的模板——它已經示範了「印量到幾筆」與「負控制分四種壞法」。

**紅點那三顆不用在這道關驗。** glitch-live 的 `verify.mjs` 已經驗過
（亮、看過會滅、重新掛載不再亮、三個已讀變數都寫回去），這裡重複只會多一個會漂的地方。

### 這一支要新做

沒有現成的。建議放 `tools/storylint.py`，輸入 `payload.json` 加軌道，輸出四項各自的
「量到幾筆／幾筆不過／哪幾筆」。**一定要印量到幾筆**——只印綠紅的話，
「軌道解析出 0 張卡」也會印綠，那正是今天修掉的那種假綠。

### 負控制

在 `payload.json` 的副本上，四項各弄壞一次，每一項都要由**該抓的那一項**抓到：

| 弄壞什麼 | 應該由誰抓到 |
|---|---|
| 軌道上某一張卡的 `background` 清成空字串 | 3-1 |
| 某一個換曲點的 `bgm` 改成空字串 | 3-2 |
| 某一句的 `voiceUrl` 拿掉 | 3-3 |
| `project.languages[0].voiceMode` 改成 `off` | 3-3 的第二半 |
| 逐字稿裡某一天的手機那幾行換成「打不開」 | 3-4 的 E |

---

## 四、三層共同的規矩

1. **每一層要能單獨跑、單獨紅。** 一支大腳本印一個總分會讓人看不出是哪一層破的。
2. **負控制跑在真資料上。** 有真的 `payload.json`／真的逐字稿就用真的。
   合成資料只用來補真資料裡湊不出來的情況，而且要註明它跟真資料哪裡不同形。
   （2026-09-12 的教訓：合成的逐字稿每天只有一個時段標頭，真的有 2–4 個，
   因此切區塊那條正規式的 bug 在合成資料上完全看不出來。）
3. **每一項都要印「量到幾筆」，不只印綠紅。** 量到 0 筆還印綠，就是假綠。
4. **不確定的差異要逐條列進版控**，不要靠「大家都知道那是擲骰」。

---

## 五、現在有什麼、缺什麼

| 層 | 工具 | 狀態 |
|---|---|---|
| 一 | `tools/pathlint.py` | **有**，而且已經涵蓋軌道斷了／選項沒接好／劇情模式走不出去 |
| 一 | `MODE=story` 的 `autoplay.mjs` | **有** |
| 二 | `tools/railcheck.py` | **有**，缺的是「允許的不一致」那份清單 |
| 二 | `score.py`（名單 6/6） | **在 /tmp**，要先搬進 `tools/` |
| 三 | 背景／BGM／語音 | **沒有**，要新做（建議 `tools/storylint.py`，靜態） |
| 三 | 手機 | **有**，`~/glitch-live/dev/check-transcript.mjs` |

---

## 六、一次完整驗收怎麼跑

順序有意義：靜態的先跑，四十分鐘那一趟只跑一次。

```bash
cd ~/glitch-vn

# 1. 靜態，秒級。這裡紅就不用跑下去了
python3 tools/pathlint.py
python3 tools/storylint.py                     # ← 這一支還要做

# 2. 負控制，在副本上跑，證明上面那兩支會紅
#    （做法見各層的「負控制」那一節）

# 3. 實跑一趟，四十分鐘
MODE=story OUT=/tmp/story-run node tools/autoplay.mjs

# 4. 拿那一趟的逐字稿驗後兩件
python3 tools/railcheck.py /tmp/story-run/transcript.txt
node ~/glitch-live/dev/check-transcript.mjs /tmp/story-run/transcript.txt

# 5. 負控制，拿這一趟的真逐字稿改
node ~/glitch-live/dev/check-transcript.mjs --self-test /tmp/story-run/transcript.txt
```

**任何一層紅就是這道關沒過**，不要用「其他兩層都綠」去折抵。
